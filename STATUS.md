# Current Status

Scope authority: what the foundation implements and what it explicitly excludes.
Ownership is in [`ARCHITECTURE.md`](ARCHITECTURE.md); verification is in
[`TESTING.md`](TESTING.md); intent is in [`GAME_DESIGN.md`](GAME_DESIGN.md);
agent routing is in [`AGENTS.md`](AGENTS.md).

> **Agent quick-find:** implemented domains are listed below. [`ARCHITECTURE.md`](ARCHITECTURE.md)
> owns the `Registry → AppState → run_tick` tower and source map; [`AGENTS.md`](AGENTS.md)
> routes changes to the owning contract. Version facts are at the bottom; keep them in
> sync with `src/core/state.rs` and `src/content/mod.rs`.

## Runtime foundation

- `AppState` owns serializable campaign state, simulation time, typed ID counters, attention settings, and independent deterministic RNG streams for operation, investigation, business, and enterprise work. Autonomous recruitment is deterministic managerial judgment and consumes no RNG.
- `Registry` owns immutable Rust-authored definitions. Runtime records and generated values belong to `AppState` and its domain owners.
- Save/load validates envelope, schema, authored content revision, registry references, cross-references, indexes, and ID high-water marks before accepting state. Compatibility is current-version only; no implicit migration or defaulting.
- [`core::simulation::run_tick`](src/core/simulation.rs) is the canonical one-minute pipeline: it processes due work in stable order and returns `TickOutcome`.
- `validate_state` provides release-safe structural checks; debug boundaries additionally run the full invariant validator.

## Implemented domains

| Domain | Capability | Owner and entry point |
| --- | --- | --- |
| World | Organizations, characters, neighborhoods, businesses, institutional profiles, membership and supervision, player designation, daily payroll (all organization-owned liquid cash is eligible and available cash is shared evenly to the cent across active members; indivisible remainder cents rotate by payroll day so stable ID order cannot create a permanent wage advantage; underpaid members with supervisors gain supervisor-directed resentment proportional to their uncovered wage share, and the player organization receives a shortfall report); read-only per-district territory-influence summaries over enterprise records | `src/world/` via `world_system`, `payroll_execution`, `territory_influence` |
| Organizational policies | Behavioral standing policies stored with organizations under per-policy monotone versions, mutated and resolved through governance so held authority cannot survive a change-away-and-back cycle; organization-level recruitment-policy changes atomically cancel only pending approvals that depended on the superseded organization setting, while mandate overrides remain authoritative; policies are consumed by recruitment autonomy and automatic legal-support retention | `src/world/` storage coordinated by `src/delegation/delegation_system`; `legal_representation_system` executes legal-support policy |
| Social | Directional relationships with source and target indexes | `src/social/` via `relationship_system` |
| Intelligence | Provenance-bearing information with typed topics, sparse typed semantic signals for facts consumed by policy (case activity, observed organization personnel presence, and approximate recurring patrol patterns), holder indexes, and one-shot source/recipient transfers that preserve both semantics and lineage without duplicating the same report; reliability/specificity quality scoring is authored once in the immutable registry and shared by every consumer rather than redefined per subsystem | `src/intelligence/` via `intelligence_system`; quality authorship in `src/registry/` |
| Reports and history | Player-facing reports, executive briefs, financial reports, Notable standing reports when the player organization's own street reputation shifts, entity-linked campaign events; executive briefs preserve historical report text and surface later financial disposition/deposit events separately rather than rewriting prior prose, rank bounded source entries by attention then newest report chronology, and keep the oldest pending decisions first within an attention class | `src/reports/` and `src/history/` |
| Finance | Typed accounts, allocator-neutral planned account openings for composite transactions, and balanced multi-account ledger | `src/finance/` via `finance_system` |
| Operations | Semantic plans (objectives, approaches, roles, participant reservations, intelligence, timing, contingencies) resolving deterministically through exposure, surveillance, and police response; effective execution ability combines crew role skill and the leader's authored domain capability through registry-authored weights; cause-specific atomic aborts for authority, deadlines, decisions, police arrival, and participant detention; after-action records distinguish tactical execution from mutable practical objective blockers; achieved surveillance persists every observed recurring patrol window as typed intervals and conservatively expands schedule windows to containing authored observation-bucket bounds instead of allowing short windows to collapse into false all-day coverage; debrief-derived district police knowledge from pre-entry police-arrival aborts; operation-originated legal heat remains anchored to the incident scene/objective rather than following later organization asset purchases into unrelated districts; operation-kind-specific repeat-target value depletion now replenishes continuously over the authored recovery window rather than jumping from depleted to full value at a time cliff; property and cash proceeds remain held until canonical disposition/deposit, with property fencing recovery adjusted downward by venue police presence using authored bounds; extraction binds the exact custody relationship authorized and never retargets a later arrest; witness pressure snapshots the exact still-pressureable case-witness set and derives patrol/exposure geography from the latest pressureable foreign witness case for every target, rather than from unrelated assets belonging to the witness or their organization, falling back only to the latest durable foreign registration if pressureability disappears in flight; business-target operations enforce intrinsic ownership, with gambling requiring a sponsor-controlled cash-intensive customer venue and take/disruption operations requiring foreign businesses; sabotage/arson disruption additionally requires an operating target | `src/operations/` via `operation_system` (authorization/start), `operation_abort` (abort lifecycle/artifacts), `operation_objective` (runtime viability), and `operation_execution` (resolution) |
| Opportunities | Information-backed discovery with an open/dismissed/expired/converted lifecycle; every discovery must contain at least one target currently actionable by its operation kind while still allowing additional contextual targets | `src/opportunities/` via `opportunity_system` |
| Decisions | Durable typed requests with recipient/context indexes, versioned resolution or explicit cancellation, attention classes; mandate revision/revocation and organization-sourced recruitment-policy changes cancel pending approvals whose frozen authority can no longer become current | `src/decisions/` via `decision_system` |
| Delegation | Organization-owned mandates with responsibility scopes, policy overrides, budget authority, revision, revocation, dependency checks, organization-policy mutation/resolution, and atomic composition with decision-owned cancellation when a governance change permanently supersedes a pending recruitment approval | `src/delegation/` via `delegation_system` |
| Recruitment | Relationship-gated recruitment, defection, cooldowns, executive approval, delegated autonomy (managers rank visible prospects by relationship support rather than random selection; when organizations contend for one prospect, the strongest currently actionable visible relationship gets first access and stable IDs break only exact ties; RequireApproval managers raise durable exception approvals; non-player organizations resolve their own queue in-pass), one exclusive route per organization-candidate pair across all channels, with a pending approval blocking autonomous and generic membership-assignment bypass while that exact approved decision remains authorized to complete the canonical reassignment; refused-approach loyalty reporting to the candidate's organization; underworld competence reputation as an authored scoring term | `src/recruitment/` via `autonomous_recruitment`, `recruitment_system`, and `scoring` |
| Enterprises | Routine criminal enterprises (protection, gambling, alcohol distribution, bookmaking, loan sharking, fencing, speakeasy, labor racketeering) with authored economics, venue-function requirements version-pinned at establishment and resumption, one live racket of a kind per location, manager authority, scheduled cycles settling balanced; chronic net loss suspends at the authored threshold, counted since the latest resumption; suspended rackets may resume or be terminally retired, preserving the exact retirement boundary for historically correct financial windows while releasing their kind/location slot for a fresh enterprise; active district casework adds a street-heat surcharge reported on appearance or change (an unchanged surcharge settles as routine cost) and rolls per-cycle vice attention that can open an originated inquiry on the racket itself with organization-held legal knowledge; financial reporting; daily delegated expansion for non-player organizations gated by police-fear posture and authored one-cycle operating-capital runway, with existing active rackets and earlier same-pass establishments reserving their claims against shared cash; planning never reads hidden live investigations and instead uses district pressure observable from the organization's own recent settled racket history, expiring that observation at the same cold-case horizon while actual settlement continues to use authoritative legal truth; viable expansion candidates must have positive zero-variance production net under that observed pressure, explicit led/other neighborhood scopes retain influence priority, specific business scopes follow, and an organization-wide Enterprise-function scope remains a broad fallback over all city neighborhoods and owned venues; economics ranks candidates within an authority tier; across organizations, one phase-wide competition gives existing district leadership priority over internal mandate specificity, then compares projected economics, with stable IDs only on exact ties; deterministic minimum-cardinality support-business composition preserves replay | `src/enterprises/` via `enterprise_execution`, `autonomous_expansion`, and reporting modules |
| Legitimate economy | Business operating economies with ownership transfer/history, scheduled cycles, authored economics including the per-kind acquisition price, accounting information, comparative reporting, sabotage/arson disruption horizons with degraded earning power for exactly the authored number of simulation minutes, chronic-loss suspension counted since the latest resumption; organizations buy independently owned businesses outright at that price by aggregating organization-owned accounted-funds accounts, with the ledger owning the exact deterministic debit allocation; purchase consideration leaves buyer-controlled spendable liquidity through the acquired business's non-liquid settlement counterparty rather than capitalizing its operating account; acquisitions open the first operating economy when none exists or restart existing books from purchase time, reanchoring active cycles and reopening suspended books without inheriting the seller's loss streak; purchases surface as Notable financial reports for the buyer | `src/economy/` via `business_economy_system`, `business_acquisition`, and reporting modules |
| Money laundering | Street-cash-to-accounted-funds conversion through owned cash-intensive fronts with an authored fee split to the front's operating account; per-cycle plausibility budget capped at an authored fraction of the front's current gross potential (sabotage-degraded books shrink it), so splitting one sum across transfers cannot exceed it; commit pins both the front's business ownership/version and its economy version, and the source must hold the laundered cash. Accounted wealth is the only money that can buy a legitimate business, so laundering throughput gates legitimate expansion | `src/finance/` via `finance_system::validate_launder_funds`; purchases via `economy::business_acquisition` |
| Reputation | Contextual per-audience organizational standing across fear/reliability/competence/treachery; sparse records created at baseline on first touch and erased as soon as every dimension returns to baseline; each dimension persists its own latest real-movement time so day-boundary decay affects only impressions at least one full campaign day old and a clamped no-op cannot refresh them; deterministic operation-consequence producers (success competence, exposure fear, violent-approach business fear; clamped scores are not reported as movement) atomically pair player-visible shifts with their Standing report; police fear from an owned racket drawing a vice inquiry throttles delegated expansion while it decays; underworld competence feeds recruitment scoring | `src/reputation/` via `reputation_system` (`apply_reputation_delta` is the single score mutation path) |
| Legal institutions | Append-only jurisdiction and patrol revision histories with historically validated timed police-response routing/presence snapshots; investigations originated by operation exposure or enterprise vice attention (only originated cases decay cold; inactivity shelves even an identified at-large lead when modeled work stops, releasing the lead investigator, while a later incident sharing shelf subject matter resumes the most relevant existing file by exact origin, then broader overlap, then recent activity, and merges every validated incident-declared subject plus every valid external notification recipient into that file rather than opening a parallel case, preserving the recipient's canonical authority-surveillance sightline); evidence graphs; conflict-free single-seat staffing with investigator-held case-activity knowledge whose typed subject is the originating operation/enterprise when one exists, so contact channels can distinguish simultaneous cases without parsing prose or exposing hidden case IDs; sequential detective review attempts across each reviewable evidence source, with detention-cancelled work retryable after restaffing and completed inconclusive attempts not looped forever; witness interviews producing at most one named statement per registered case witness, with confidence bands and cooperation discounts authored in legal registry content; external witness-cooperation changes and custody-forced detective work cancellation/lead release invalidate case-dependent plans or staffing without refreshing institutional cold-case activity; evidence-threshold arrests through both direct and autonomous paths, with autonomous custody scanning active LawEnforcement-owned investigations; both paths count independent non-weak/non-questionable evidence sources not known inadmissible toward the authored corroboration bar, so derived analysis adds no new source and multiple witness/informant records naming the same source count once, while source-less primary evidence represents distinct gathered facts; non-police LegalAuthority files do not exercise custody authority; when several police files independently qualify one character, autonomous custody prefers more independent qualifying sources, then more Strong/Direct independent sources, before investigation ID; autonomous custody is one-shot per case/person pair so unchanged evidence cannot create a release/re-arrest loop, and every arrest detention ends at the authored maximum when no earlier canonical release occurs because bail, trial, sentence custody, and court procedure remain outside the modeled foundation even though prosecution referral/review can continue after release; custody atomically preempts the detainee's live operation, detective-work, investigation-lead, prosecution, and retained-counsel assignments instead of allowing unavailable characters to remain booked; automatic legal support is resolved before the detainee's one-time informant decision, and active counsel reduces the authored custodial flip chance while informant disclosures preserve case provenance and handler conflicts constrain later membership reassignment; representation (direct retention and organization-policy automatic retention may aggregate sponsor-owned liquid accounts, with exact allocation only in the ledger; mandate-sourced automatic retention and explicitly delegated retention are constrained to the mandate's Legal scope, budget account, and current budget window; automatic policy chooses the strongest currently usable LegalServices counsel by LegalKnowledge rather than contact creation order, consults organization policy under the defendant's supervisor's mandate override, and concludes after custody ends, while explicitly commanded retention persists until leadership ends it unless counsel becomes unavailable through custody); prosecution referral/review with replaceable current prosecutor staffing, defendant-specific referred evidence, post-release review, and referral/resolution actor provenance, while the final terminal review releases the originating detention when no other prosecutor office still reviews that arrest | `src/legal/` named system modules; invariant validation split per aggregate under `src/core/invariants/legal/` |
| Institutional contacts | Person-mediated Police, Legal, Political, Press, Labor, and Professional channels with provenance-preserving disclosure and a read-only pending-disclosure offer surface ordered newest-first so a current query does not replay stale status before fresher held information; establishment requires a usable social basis, with resentment alone insufficient to create access | `src/contacts/` via `contact_system` |

## Cross-cutting guarantees

- One owning system and one canonical path per consequential mutation.
- Fallible multi-record operations validate references, ownership, lifecycle, permissions, capacity, arithmetic, and versions before commit.
- Rejected operations preserve authoritative state except for explicitly modeled diagnostics or audit records.
- Derived indexes and summaries are maintained only from authoritative records and checked for consistency. They are omitted from save bytes and rebuilt deterministically from records during restore before invariant validation. An ID obtained from an owner-maintained index must resolve to its authoritative record; missing indexed targets are invalid state, not ordinary absence to filter away.
- Result-affecting randomness uses serialized state-owned streams; ordered work uses explicit stable ordering.
- Player-visible information is separate from hidden world truth. Reports point to information records; adapters and harness diagnostics do not grant omniscience.
- Filesystem, process, network, and UI effects remain outside core systems.
- The harness exercises production paths with authored fixtures and deterministic policy comparisons. It does not prove human comprehension or interface quality.

## Explicit exclusions

The foundation does not model:

- detailed city geography, transport topology, population simulation, broad institution networks, supply chains, inventory quantities, negotiated or dynamic acquisition pricing, market competition, or dynamic citywide supply and demand;
- broader employment, wages, family ties, secrets, injuries, general character needs, specialist affiliations, or universal autonomous behavior;
- delegated role staffing, broad resource competition beyond overlapping character assignments, diplomacy, territory strategy, multi-step rival planning, or general campaign-pressure generation;
- durable receivable/payable obligations for unpaid AI-organization payroll: a shortfall models its consequence through crew resentment (and, for the player organization, a shortfall report), not as carried debt;
- reusable equipment and vehicles, tactical movement, combat control, pursuit, dispatch capacity, casualties, injuries, asset damage, or condition-specific tactical consequences;
- autonomous investigative lead generation beyond modeled evidence review and case-graph work, case merging, charging, bail, trial, conviction, acquittal, court procedure, corruption, political procedure, press behavior, or labor institutions;
- a universal approval flag, generic legal-pressure meter, or generic mission-card generator. Modeled approvals, legal pressure, and opportunities remain typed and domain-owned.

These are scope boundaries, not evidence for unmodeled design goals.

## Version facts — single source of truth for persistence compat

These two numbers gate `core::persistence::restore_save` (`src/core/persistence.rs`):
mismatched saves are rejected. Keep them in sync with the owners.

The current authored content revision is 43.

The current in-memory state schema version is 84.

The compiled operation vocabulary contains only objectives, constraints, and contingencies with corresponding execution inputs and outcomes, and the policy vocabulary contains only settings with a consuming system. Unsupported tactical or governance axes are not represented as inert plan fields.
